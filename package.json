{
  "name": "package-add",
  "version": "1.0.2",
  "description": "Programmatically install packages. Supports yarn, npm, and pnpm.",
  "main": "./build/index.js",
  "author": "Connor White",
  "license": "MIT",
  "scripts": {
    "build": "bob build",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@bconnorwhite/exec": "^3.0.1",
    "@bconnorwhite/package": "^2.1.1"
  },
  "devDependencies": {
    "@bconnorwhite/bob": "^1.6.0",
    "@types/node": "^14.0.27"
  }
}
